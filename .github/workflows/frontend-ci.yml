name: Frontend CI

on:
  push:
    branches:
      - main
      - develop
    paths:
      - "frontend/**"
      - ".github/workflows/frontend-ci.yml"
  pull_request:
    paths:
      - "frontend/**"
      - ".github/workflows/frontend-ci.yml"

permissions:
  contents: read

jobs:
  frontend-ci:
    name: Frontend Quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend deps
        run: |
          cd frontend
          npm ci

      - name: Run ESLint
        run: |
          cd frontend
          npm run lint

      - name: Run TypeScript typecheck
        run: |
          cd frontend
          npm run typecheck

      - name: Run Prettier format check
        run: |
          cd frontend
          npm run format:check

      - name: Run unit tests with coverage
        run: |
          cd frontend
          npm run test:unit:coverage

      - name: Audit dependencies
        run: |
          cd frontend
          npm audit || true

      - name: Build frontend
        run: |
          cd frontend
          npm run build

      - name: Install Playwright browsers
        run: |
          cd frontend
          npx playwright install --with-deps chromium

      - name: Run smoke E2E tests
        run: |
          cd frontend
          npx playwright test --config playwright.ci.config.ts tests/e2e/route_startup.spec.ts tests/e2e/app_smoke.spec.ts --reporter=line --project=chromium
